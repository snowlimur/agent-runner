# Instructions

- After every code change in the `agent-cli` directory, you MUST run `task lint` from the `agent-cli` directory (for example: `cd agent-cli && task lint`).
- All comments and instructions in the project MUST be in English.
